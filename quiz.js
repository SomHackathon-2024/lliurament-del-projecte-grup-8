
/**Array.from(document.querySelectorAll('input[type=radio]')).forEach(item => {
    item.addEventListener('click', e => {
        let contFM = document.querySelectorAll('input[type=radio][value=Familiar]:checked').length;
        let contRX = document.querySelectorAll('input[type=radio][value=Relax]:checked').length;
        let contJV = document.querySelectorAll('input[type=radio][value=Joves]:checked').length;
        let contEX = document.querySelectorAll('input[type=radio][value=Explorador]:checked').length;
    })
}) */
/** Hemos intendado hacer contadores para contar cada opción seleccionada y al final se determinaria a cual pagina de resultado iria */

